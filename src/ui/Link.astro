---
const { text, href, current, post, fm, ...rest } = Astro.props;
---

<a
  href={href}
  {...rest}
  class={`font-normal ${
    current &&
    "text-slate-600 dark:text-slate-300 no-underline cursor-default pointer-events-none"
  } ${post && "no-underline font-medium"} ${fm && `font-medium`}`}>{text}</a
>
